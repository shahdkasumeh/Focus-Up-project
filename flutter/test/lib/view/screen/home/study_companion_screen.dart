import 'package:flutter/widgets.dart';

class StudyCompanionScreen extends StatelessWidget {
  const StudyCompanionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
