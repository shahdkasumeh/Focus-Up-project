import 'package:flutter/widgets.dart';

class HallSmokeFreeScreen extends StatelessWidget {
  const HallSmokeFreeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
